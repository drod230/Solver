from typing import Tuple

# Function to interploate on curve
def InterpolateArray(x: list, y:list) -> Tuple[float, float]:
    """_summary_
    
    Args:
        x (list): _description_
        y (list): _description_
    """
    pass
    x = 1
    y = 1

    return (x, y)
